server:
  port: 8001
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/sakila
    username: root
    password: aski123
    driver-class-name: com.mysql.cj.jdbc.Driver
#spring.jpa.show-sql: true
  cloud:
    config:
      enabled: false