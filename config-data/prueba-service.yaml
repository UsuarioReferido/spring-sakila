server:
  port: 8002
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/pruebas
    username: root
    password: aski123
    driver-class-name: com.mysql.cj.jdbc.Driver